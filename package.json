{
  "name": "ai-editor-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered, Version-Controlled Document Editor",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=frontend && npm install --workspace=backend",
    "dev": "node run-all.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  }
}
